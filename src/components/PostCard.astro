<!-- ---
import { Markdown } from '@astrojs/markdown-remark';
const { title, excerpt, slug, publishedAt, thumbnail } = Astro.props.post;
---

<article class="bg-white shadow-md rounded-lg overflow-hidden">
  {thumbnail && (
    <img src={thumbnail.url} alt={thumbnail.alt} class="w-full h-48 object-cover" />
  )}
  <div class="p-4">
    <h2 class="text-xl font-semibold text-gray-800">
      <a href={`/blog/${slug}`} class="hover:text-blue-500">{title}</a>
    </h2>
    <p class="text-gray-600 text-sm">{new Date(publishedAt).toLocaleDateString()}</p>
    <p class="mt-2 text-gray-700">{excerpt}</p>
  </div>
</article> -->
